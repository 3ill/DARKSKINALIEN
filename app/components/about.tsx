import React from "react";
import Title from "./title";

const About = () => {
  return (
    <section id="about" className={`flex h-screen w-full pt-0 sm:pt-[150px]`}>
      <Title title={`About Me`} />
    </section>
  );
};
export default About;
