import React from "react";
import Title from "../components/title";

const Connect = () => {
  return (
    <section className={`flex w-full flex-col pt-[150px] sm:pt-[150px]`}>
      <Title title="connect" />

      <div className={`flex w-full flex-col items-center lg:flex-row`}></div>
    </section>
  );
};

export default Connect;
